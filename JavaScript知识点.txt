JS发展史
1992年 NOMbase公司 发明C--用于表单校验，后来更名ScriptEase
1995年 Netscape(网景公司)开发了LiveScript 和Sun公司一起修改，然后命名JavaScript
1996年 微软抄袭JavaScript开发JScript
1996年 ECMA制定ECMAScript标准
JavaScript=ECMAScript+JavaScript自己特有的东西（BOM+DOM）




ECMAScrip：客户端脚本语言标准

1、基本语法
与HTML结合发放
内部JS  外部JS

内部JS
<script>
	可以写在任意位置，与CSS不一样，但有执行顺序
</script>

外部样式 通过
<script> src属性引入JS

<script>可以定义多个标签

2、注释
单行注释//
多行注释/* */


3、数据类型
(1)原始数据类型
（1）number 整数 小数 NAN不是数字的数字类型
（2）string ‘’单引号 和“”双引号都是字符串。
（3）boolean	true false
（4）null      一个对象对空的占位符
（5）undefined  未定义，如果一个变量没有初始化值，则默认为undefined

（1）引用数据类型（对象）
Function
1、创建
(1)var fun=new function("a","b","方法体");//不常用
（2）fuction fun(a,b)
{
	//方法体
	alert(a+b);
}
（3）var fun3=function(a,b)
{
alert(a,b)
}
2、方法
3、属性
length,形参的个数

4、特点
特点：方法创建时，形参的类型不用写
在JS中，调用之和名称有关，与形参列表无关
在方法中有一个隐藏的内置对象 argument


Array
1、创建
（1）var arr= new Array(长度)
（2）var arr= new Array(参数列表)
（3）var arr= [参数列表]  
2、方法
3、属性
4、特点
Array数组中，元素类型可变
Array数组中，长度可变

Boolean
Date
1、创建
var date=new Date()
2、方法
localstring返回本地时间格式

3、
4、


Math数学对像
不用创建，直接使用
Math.pow()
Math.sqrt()
Math.PI属性


Number
String
RegExp
正则表达式对象
字符
\d单个数组字符
\w单个单词字符
量词
？表示0词或1词
* 0次或多次
+1次或多次
{m,n} 大于m小于n次
m缺省  最多次


创建正则表达式对象
var reg=RegExp(正则表达式)
var reg=/正则表达式/
开始^
结束$


Global

变量（一小块存储数据的内存空间）Java是强类型语言 JS是弱类型语言
语法：var 变量名=初始值


运算符

自增自减运算符
算术运算符
逻辑运算符
赋值运算符


流程控制语句
if else
switch(什么类型都可以接受)
switch(a)
case 1: alert("number"）;break;
case ture: alert("boolean");break;
case null: alert("null");break;
case undifind: alert("undefind");break;
case "abc"：alert("string");brea;

for 
while
do-while




DOM对象

var 变量 document.getElemtByID()

操作，修改属性值，
变量.属性
修改标签体
变量.innerHTML


document.write(在页面上输出的内容)

alert（弹出框）


事件
某些组件被执行了某些操作后触发了某些代码

事件 onclick点击事件
onclick=“alert("我被点了")”
第一种方法  onclick
第二种先获取id   变量=document.getElemtById(id)  变量.click 此种方法降低耦合度


BOM

windows
方法

location
方法
reload 刷新当前页面
href  回去href属性
onclick=function(){
 location.href="http://wwwbaidu.com"
}

History历史记录对象
length返回当前窗口的历史记录

方法 back
forward
go


DOM对象





